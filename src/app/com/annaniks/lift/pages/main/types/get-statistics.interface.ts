export interface GetStatisticsInterface {
  accountId: number; // Число
  startDate: string; // Дата в виде строки (ISO date): YYYY-MM-DDTHH:mm:ss.sssZ
  endDate: string;   // Дата в виде строки (ISO date): YYYY-MM-DDTHH:mm:ss.sssZ
}
